@model IEnumerable<QuizApp.Models.Course>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ID }, new { @class = "btnEdit" }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }, new { @class = "btnView" }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID }, new { @class = "btnDel" })
        </td>
    </tr>
}

</table>
<p>
    @Html.ActionLink(" Add Course", "Create", null, new { @class = "btn btn-info btn-sm;glyphicon glyphicon-plus" })
</p>
